# 文章博士（Monjo-Hakase）— リブート目標（to-be）

> この文書は「リブート後にこうしたい」という目標・方針を書く場所。決定が変わったら更新する。
> 現状（引き継いだ旧システムの実態）は [`as-is.md`](./as-is.md) を参照（そちらは固定・不変）。

## 目的

旧「文章博士」の思想 ―― *機械が係り受けを解析できる文＝分かりやすい技術文* ―― を引き継ぎつつ、
2010 年代前半で止まった実装を現代的な形で再構築（リブート）する。

## 引き継ぐもの / 捨てるもの

| | 内容 |
|---|---|
| **引き継ぐ** | 校正の中核思想、7 種のエラー分類、係り受けベースの読みやすさ評価、フリー/OSS での公開 |
| **捨てる** | Perl CGI 前提、死んだ第三者ウィジェット（忍者/はてな/Yahoo!/埋め込みBBS）、旧 GA（ga.js）、HTTP 平文運用 |
| **要検討** | 解析エンジンを CaboCha/MeCab/jcorrect のまま使うか、現行の形態素・係り受け解析器へ載せ替えるか |

## 非機能要件（方針）

- **HTTPS 必須。** TLS 終端をどこで持つか（リバースプロキシ / ロードバランサ / アプリ）を決める。
- **コマンドインジェクションを設計段階で排除。** 外部プロセス呼び出しはシェルを経由せず、リスト形式の引数渡し（`IPC::Open2` 相当、または各言語の safe-exec）で行う。ユーザー入力を文字列連結でシェルに渡さない。
- **テスト可能な構成。** 校正ロジックを CGI 出力から分離し、入力→エラー配列 の純粋関数として単体テストできるようにする。
- **再現可能な実行環境。** 解析エンジンの依存（辞書・バージョン）をコンテナ等で固定する。

## アーキテクチャ候補（未決）

以下は選択肢のたたき台。決めたらここを更新する。

1. **最小改修案** — 既存 Perl をベースに、インジェクション修正・TLS・トラッキング除去だけ行う。移行コスト最小、負債は残る。
2. **API + フロント分離案** — 校正ロジックを HTTP API（例: Python/Go/Node）として切り出し、フロントを別途用意。
3. **コンテナ化案** — 解析エンジン込みで Docker 化し、どこでも同一に動く校正サービスとして再配布。

## 未決事項（TODO で埋めていく）

- [ ] 実装言語・フレームワークの決定
- [ ] 解析エンジン: CaboCha/MeCab 継続 or 載せ替え（候補調査）
- [ ] jcorrect（GPL）を使い続けるか → 使うならライセンス整理（GPL 汚染範囲の確認）
- [ ] ホスティング先（現行さくら VPS を継続 / 別環境）
- [ ] ドメイン（`monjo.lunark.org` 継続 / 新規）
- [ ] 旧サービスの扱い（凍結 / リダイレクト / 併存）

## マイルストーン（ドラフト）

1. **現状記録**（← 完了: `as-is.md`）
2. リブート方針の確定（この文書を埋める）
3. 校正ロジックの移植 + 単体テスト
4. TLS 込みの最小構成デプロイ
5. フロント刷新・旧ウィジェット除去
